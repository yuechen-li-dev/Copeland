module

func parseNumber(text: string) -> number ! ParseError
entry:
  return ok 1

func caller(text: string) -> number ! ParseError
locals:
  const x: number
entry:
  store x, propagate call parseNumber(text) to function-return
  return ok x
