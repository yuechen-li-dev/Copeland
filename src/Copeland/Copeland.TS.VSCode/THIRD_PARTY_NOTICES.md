# Third-party notices

This VSIX bundles `vscode-languageclient` and its runtime dependencies. Their
license files and copyright notices are retained within the corresponding
`node_modules` package directories in the VSIX.

The Copeland compiler and language server are not bundled in the VSIX; they are
resolved through the separately installed `tscl` command.
