using Silk.NET.Vulkan;
using Silk.NET.Windowing;

namespace Aurelian.Graphics.Vulkan;

public static class VulkanPackageSmoke
{
    public static string VulkanApiName => nameof(Vk);
    public static string WindowOptionsName => nameof(WindowOptions);
}
