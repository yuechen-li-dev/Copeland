# Review finding

The compiler-derived graph makes the self-transition on `Still`, the two recovery routes from `Moving`, and the terminal `Crash` path visible without restating the flow declaration.
