# CSV-shaped stream browser proof

`src/Main.tsx` is literal `csv overlay` Copeland source. TSPack owns package
materialization, build, server lifecycle, Playwright assertions, screenshot,
and cleanup. The scenario confirms all four generated hosts and that the
highest-z modal tooltip is topmost at an overlap point.
