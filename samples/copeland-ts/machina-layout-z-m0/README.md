# Machina layout z M0 fixtures

This focused fixture demonstrates default paint order, bounded local z,
semantic layers, and stream-bound React contents. The authoritative compiler
coverage is `LayoutZM0Tests`; a browser overlap scenario is intentionally not
duplicated here until the existing TSPack scenario surface exposes generated
layout hosts directly.
