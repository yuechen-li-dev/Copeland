# Copeland table derivation M0 samples

`01-center/Scene.ts` is the nested-`with` dogfood scene. `02-csv-equivalent/Scene.ts` has the same derivation plan through a CSV `derivations` list. `10-browser-proof/` is the TSPack/Playwright rectangle proof.
