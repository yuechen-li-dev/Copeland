# Repository decisions

Repository-wide architectural decisions live here. Historical milestone narratives remain under subsystem `history` directories or `docs/migrations` so current doctrine stays easy to locate.
